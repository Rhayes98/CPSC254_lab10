echo "In our project we worked in Java and we named our files after the classes they defined as a single string with no spaces. In one case we used camel case and in another we used an underscore"
echo
echo "ls -l"
echo

ls -l
